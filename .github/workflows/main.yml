name: Windows-RDP-Tmate

on: workflow_dispatch

jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 360

    steps:
    - name: Enable RDP and Set Password
      run: |
        # تفعيل الـ RDP وتغيير الباسورد
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0
        Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
        $Password = ConvertTo-SecureString "Pass2026!" -AsPlainText -Force
        Set-LocalUser -Name "runneradmin" -Password $Password

    - name: Install and Run Tmate
      run: |
        # تحميل أداة tmate وتشغيل النفق تلقائيًا بدون حسابات
        choco install tmate -y
        tmate -F
