name: Windows-RDP-LocalTunnel

on: workflow_dispatch

jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 360

    steps:
    - name: Enable RDP and Set Password
      run: |
        # تفعيل الـ RDP وتغيير الباسورد
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0
        Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
        $Password = ConvertTo-SecureString "Pass2026!" -AsPlainText -Force
        Set-LocalUser -Name "runneradmin" -Password $Password

    - name: Install Node and LocalTunnel
      run: |
        # تثبيت أداة لوكال تانل عبر npm المدمج في السيرفر
        npm install -g localtunnel

    - name: Start Tunnel
      run: |
        # فتح النفق وتثبيته على منفذ الـ RDP الافتراضي 3389
        lt --port 3389
